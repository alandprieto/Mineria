[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mineria-tp"
version = "0.1.0"
description = "Utilidades reutilizables del TP Integrador de Mineria de Datos"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]